node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example

# editor / logs
.vscode/*
!.vscode/extensions.json
.idea
*.log
npm-debug.log*

# TS build info
*.tsbuildinfo

# 로컬 서버 데이터(지난 수업 미러 등) — 수업 기록은 커밋하지 않는다
.kv-data/

# Claude Code local settings (per-machine; not shared)
.claude/settings.local.json
.omc

# bkit plugin local state (per-machine)
.bkit/

# 대용량 미연결 스티커 스테이징 리소스(앱 미참조) — 레포 부풀림 방지, 디스크에만 보관
public/assets/deco/tag/1-3차 태그 스티커 리소스/
